#include<iostream>
#include<conio.h>
#include<math.h>

int main()
{
using namespace std;
int x=1;
float a;
a=pow(((abs(x-1)+pow(exp(x),-x)-12.34)/(log10(sqrt(abs(x)))-cos(x*x*x))),-0.4);
cout<< a;
_getch();
return 0;
}

